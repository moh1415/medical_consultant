 <%= render 'home/nav' %>
<br><br><br>
<div class="container">
<div class='col-md-10 text-center'>
<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control mb-4" %>
  </div>
<div class="form-group">
    <%= f.label :full_name %><br />
    <%= f.text_field :fullname, autofocus: true ,class: "form-control mb-4"%>
  </div>
  <div class="form-group">
    <%= f.label :Phone_number %><br />
    <%= f.text_field :phone_number, autofocus: true ,class: "form-control mb-4" %>
  </div>


     <%= f.radio_button :is_role, 'patient', :checked => true %> 
      <%= label :is_role, 'Patient' %>
    <%= f.radio_button :is_role, 'doctor' %>
     <%= label :is_role, 'Doctor' %>



  <div class="form-group">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password", class: "form-control mb-4" %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control mb-4" %>
  </div>

  <div class="form-group">
    <%= f.submit "Sign up", class:"btn btn-info btn-block" %>
  </div>
<% end %>
<br><br><br><br>
<%# <%= render "devise/shared/links" %> 
</div>
</div>